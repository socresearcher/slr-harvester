# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
.Python
env/
venv/
.venv/
ENV/
pip-wheel-metadata/
*.egg-info/
*.egg
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
.coverage.*

# IDEs / OS
.DS_Store
.idea/
.vscode/
.claude/
Thumbs.db

# Project data folders
projects/
results/
query_history.json
search_log.json
slr_global_tags.json
tag_aliases.json
tags_config.json
gui_settings.json
slr_gui_settings.json

# Local config
slr_config.json
api_keys.txt
projects.json

# SLR Harvester Web — local backup copies (see slr-harvester_web/backup-file.ps1)
slr-harvester_web/backups/

# SLR Harvester Web — dev-only planning docs, not part of the public app
slr-harvester_web/TASKS.md
slr-harvester_web/SUGGESTIONS.md

# Wrangler-Zwischenspeicher (entsteht beim Ausfuehren von wrangler in diesem Ordner)
.wrangler/

# Dev-only AI-agent instructions, not part of the public repo
CLAUDE.md

# Internal security planning notes — never publish a roadmap of unfixed gaps
SECURITY_PLAN.md

# Interne Deployment-Dokumentation (SLR Harvester) — beschreibt Projektkennungen,
# Datenbankstruktur und offene Sicherheitspunkte; gehoert nicht in ein oeffentliches Repo.
SUPABASE.md
INFRASTRUKTUR.md

# Internes Arbeitspapier samt Satzumgebung — NIEMALS veroeffentlichen.
# Der Ordner enthaelt das Papier (PAPER.md), die daraus erzeugte Satzfassung,
# das Titelbild und die fertigen PDF/HTML-Ausgaben. Er benennt offene
# Sicherheitspunkte und Kontenstrukturen und gehoert unter keinen Umstaenden
# in ein oeffentliches Repository.
paper/

# Frueherer Ablageort derselben Datei (jetzt unter paper/PAPER.md).
# Eintrag bleibt, damit eine alte Arbeitskopie sie nicht doch noch mitnimmt.
PAPER.md

# Windows-Ordnersymbol (lokale Darstellung, nichts fuer das Repo)
desktop.ini
*.ico
